\documentclass{article}
\usepackage{listings}             % Include the listings-package
\usepackage[top=1in, bottom=1.5in, left=0.5in, right=1in]{geometry}
\begin{document}
\lstset{language=Haskell}          % Set your language (you can change the language for each code-block optionally)

\lstinputlisting{portOpt.hs}

\lstinputlisting{PSO.hs}

% \begin{lstlisting}[frame=single]  % Start your code-block
% for i:=maxint to 0 do
% begin
% { do nothing }
% end;
% Write(’Case insensitive ’);
% Write(’Pascal keywords.’);
% \end{lstlisting}

\end{document}